{
  "version": 2,
  "installCommand": "rm -rf node_modules package-lock.json && npm install --include=dev --include=optional --force && npm install @rollup/rollup-linux-x64-gnu@4.24.4",
  "buildCommand": "ROLLUP_NO_NATIVE=true npm run build:safe",
  "outputDirectory": "dist/public",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/server"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
